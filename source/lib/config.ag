guard Handler: "git";

guard Domain: {
	root?: string,
	key?: string,
	cert?: string,
	pass?: string,
	host?: string,
	handler?: Handler,
	indices?: boolean,
	routing?: boolean
};

guard Options: {
	domains?: Domain[],
	http?: number,
	https?: number,
	sign?: boolean
};
